#include <bits/stdc++.h>
using namespace std;


int a, b, c ;

int main(){

    cin >> a >> b >> c;


    if( (a*b) > c  ){
        cout << ((a*b) -c);
    }else{
        cout << 0;
    }










}