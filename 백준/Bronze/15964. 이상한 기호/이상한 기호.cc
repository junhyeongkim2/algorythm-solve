#include <bits/stdc++.h>
using namespace std;


int N,K;
vector<string>ans;


int main(){

    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int a,b;
    cin >> a >> b;

    cout << (a+b) * (a-b);







    return 0;
}