#include <iostream>
#include <algorithm>
#include <math.h>
using namespace std;

int main() {

    double N ;
    cin >> N;

    cout<<fixed;
    cout.precision(5);


    cout << M_PI * pow(N,2) << "\n";
    cout << (double)2 * pow(N,2);















}