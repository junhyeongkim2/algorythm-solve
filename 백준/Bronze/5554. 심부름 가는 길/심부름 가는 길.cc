#include <bits/stdc++.h>

using namespace std;

int a , b, c, d;

int main(){

    cin >> a >> b >> c>> d;

    int sum = 0;

    sum = a+b+c+d;

    int e = sum / 60;
    int f = sum % 60;

    cout << e << "\n" << f;














}